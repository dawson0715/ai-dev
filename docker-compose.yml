services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

#  api:
#    build: ./apps/api
#    restart: unless-stopped
#    ports:
#      - "3000:3000"
#    environment:
#      - MONGO_URL=mongodb://mongo:27017/agent
#      - CLICKUP_TOKEN=${CLICKUP_TOKEN}
#      - GITLAB_SERVICE_ACCOUNTS=${GITLAB_SERVICE_ACCOUNTS}
#      - RUNNING_JOB_STALE_MS=${RUNNING_JOB_STALE_MS:-3600000}
#    volumes:
#      - ./opt/computer:/opt/computer
#    depends_on:
#      - mongo
#
#  worker:
#    build: ./apps/worker
#    restart: unless-stopped
#    environment:
#      - MONGO_URL=mongodb://mongo:27017/agent
#      - CLICKUP_TOKEN=${CLICKUP_TOKEN}
#      - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN}
#      - GITLAB_SERVICE_ACCOUNTS=${GITLAB_SERVICE_ACCOUNTS}
#      - POLL_INTERVAL_MS=${POLL_INTERVAL_MS:-30000}
#      - WORKER_CONCURRENCY=${WORKER_CONCURRENCY:-4}
#      - JOB_HEARTBEAT_MS=${JOB_HEARTBEAT_MS:-30000}
#    volumes:
#      - ./opt/computer:/opt/computer
#    depends_on:
#      - mongo

volumes:
  mongo_data:
